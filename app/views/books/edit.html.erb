<h1>Editing Book</h1>

<div class ="error" >
  <% if @book.errors.any? %>
    <%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:
    <% @book.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>
</div>

<%= form_with model:@book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %></br>

  <div class="submit">
    <%= f.submit 'Update Book' %>
  </div>
<% end %>


   <P class ="button2"><%= link_to 'Show', book_path(@book.id) %> | <%= link_to 'Back', books_path %></P>
